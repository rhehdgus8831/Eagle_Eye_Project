server:
  port: 9001


spring:
  # email setting
  #mail:
   # host: smtp.naver.com
    #port: 465
    #username: * # github ?? ??
    #password: * # github ?? ??
    #protocol: smtp
    #properties:
      #mail:
        #smtp:
          #auth: true
          #ssl:
            #enable: true

  # database setting
  datasource:
    url: jdbc:mariadb://localhost:3306/dashboard
    username: root
    password: mariadb
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    # DBMS dialect setting
    database-platform: org.hibernate.dialect.MariaDB106Dialect
    hibernate:
      # ddl
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true # SQL log
    database: mysql

# log level setting
logging:
  level:
    root: info
    com.study.event: debug
    org.hibernate.SQL: debug

game:
  border:
    latitude: 38.0 # 경계선은 북위 38.0도선이다.
